pipeline {
  agent any
  stages {
    stage('') {
      steps {
        git(credentialsId: 'cddd', url: 'http://jorge.valencia@repowe.eastus.cloudapp.azure.com/scm/we/pinera-io.git')
      }
    }
  }
}